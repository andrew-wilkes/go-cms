import * as types from "./types";
export declare function getManifest(registryBase: string, repo: string, tag: string, username?: string, password?: string, options?: any): Promise<types.ImageManifest>;
